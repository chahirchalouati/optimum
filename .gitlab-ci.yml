stages:
  - build
  - docker
  - deploy

.changes:
  only:
    changes:
      - .gitlab-ci.yml

build-maven:
  stage: build
  image:
    name: nginx
    pull_policy: if-not-present
  script:
    - echo "HELLO WORLD !!!"
  tags:
    - dev

build-docker:
  stage: docker
  image:
    name: nginx
    pull_policy: if-not-present
  script:
    - echo "HELLO WORLD !!!"
  tags:
    - dev

deploy-compose:
  stage: deploy
  image:
    name: nginx
    pull_policy: if-not-present
  script:
    - echo "HELLO WORLD !!!"
  tags:
    - dev

deploy-k8s:
  stage: deploy
  image:
    name: nginx
    pull_policy: if-not-present
  script:
    - echo "HELLO WORLD !!!"
  tags:
    - dev

