variables:
  MODULE_NAME: audit-service

.changes:
  only:
    refs:
      - .gitlab-ci-audit.yml

audit-build:
  extends:
    - .build

audit-docker:
  extends:
    - .docker

audit-deploy:
  extends:
    - .deploy