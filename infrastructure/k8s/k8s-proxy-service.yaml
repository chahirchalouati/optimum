apiVersion: apps/v1
kind: Deployment
metadata:
  name: proxy-service
  labels:
    app: proxy-service
spec:
  template:
    metadata:
      name: proxy-service
      labels:
        app: proxy-service
    spec:
      containers:
        - name: proxy-service
          image: ghcr.io/chahirchalouati/proxy-service:2
          env:
            - name: SPRING_APPLICATION_JSON
              value: '{
              "spring.cloud.config.uri": "http://configuration-service:8888",
              "spring.data.mongodb.host": "mongodb",
              "spring.data.mongodb.port": "27017",
              "spring.profiles.active":"prod"
              }'
      restartPolicy: Always
      imagePullSecrets:
        - name: myregistrykey2
  selector:
    matchLabels:
      app: proxy-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: proxy-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: proxy-service
  minReplicas: 1
  maxReplicas: 4
---
apiVersion: v1
kind: Service
metadata:
  name: proxy-service
spec:
  selector:
    app: proxy-service
  ports:
    - port: 9000
      targetPort: 9000
  type: LoadBalancer