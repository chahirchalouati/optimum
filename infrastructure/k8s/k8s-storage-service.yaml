apiVersion: apps/v1
kind: Deployment
metadata:
  name: storage-service
  labels:
    app: storage-service
spec:
  template:
    metadata:
      name: storage-service
      labels:
        app: storage-service
    spec:
      containers:
        - name: storage-service
          image: ghcr.io/chahirchalouati/storage-service:2
          env:
            - name: SPRING_APPLICATION_JSON
              value: '{
              "spring.cloud.config.uri": "http://configuration-service:8888",
              "spring.data.mongodb.host": "mongodb",
              "spring.data.mongodb.port": "27017",
              "spring.profiles.active":"prod"
              }'
      restartPolicy: Always
      imagePullSecrets:
        - name: myregistrykey2
  selector:
    matchLabels:
      app: storage-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: storage-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: storage-service
  minReplicas: 1
  maxReplicas: 4
---
apiVersion: v1
kind: Service
metadata:
  name: storage-service
spec:
  selector:
    app: storage-service
  ports:
    - port: 9000
      targetPort: 9000
  type: NodePort