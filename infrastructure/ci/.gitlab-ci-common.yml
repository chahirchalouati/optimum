variables:
  MODULE_NAME: common

.changes:
  only:
    changes:
      - .gitlab-ci-common.yml
      - .gitlab-ci.yml
      - $MODULE_NAME/**
      - .gitlab-ci-common.yml
      - .gitlab-ci.yml

common-build:
  extends:
    - .build

common-docker:
  extends:
    - .docker

common-deploy:
  extends:
    - .deploy
