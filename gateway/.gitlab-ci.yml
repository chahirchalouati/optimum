# Update n.0


proxy-service-build:
  variables:
    MODULE_NAME: proxy-service
  extends:
    - .build

proxy-service-docker:
  variables:
    MODULE_NAME: proxy-service
  extends:
    - .docker

proxy-service-deploy:
  variables:
    MODULE_NAME: proxy-service
  extends:
    - .deploy

